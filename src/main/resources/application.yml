#全局配置
mybatis:
  mapper-locations: classpath*:/mapper/*.xml
  type-aliases-package: com.liu.web.entity,com.dtdream.dingplus.alert.entity
  mapper-scan:
    - com.liu.web.dao.**
    - com.liu.web.dao
  url: "jdbc:mysql://127.0.0.1:3306/test?useUnicode=true&characterEncoding=UTF8"
  username: "root"
  password: "123456"

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/test
    username: root
    password: 123456
    driver-class-name: com.mysql.jdbc.Driver
  main:
    banner-mode: "off"

metric:
  config:
    name: loadOne
